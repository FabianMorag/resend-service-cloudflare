{
  "name": "resend",
  "version": "0.0.0",
  "private": true,
  "eslintConfig": {
    "extends":"standard"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "vitest": "1.3.0",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "hono": "^4.4.6",
    "resend": "^3.3.0",
    "standard": "^17.1.0"
  }
}